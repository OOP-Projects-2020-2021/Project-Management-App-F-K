import javax.swing.*;
import view.team.single_team.TeamFrame;

public class Main {
  public static void main(String[] args) {
    // JFrame signInFrame = new SignInFrame();
    //JFrame home = new TeamSettingsFrame();
    JFrame home = new TeamFrame(new JFrame());
  }
}
